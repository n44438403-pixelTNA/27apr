--- components/PdfView.tsx
+++ components/PdfView.tsx
@@ -2125,23 +2125,18 @@
                                                                     allow="autoplay"
                                                                     sandbox="allow-scripts allow-same-origin allow-forms allow-popups-to-escape-sandbox"
                                                                     style={
-                                                                        (pdfRotation === 90 || pdfRotation === 270)
-                                                                            ? {
-                                                                                width: '100vh',
-                                                                                height: '100vw',
-                                                                                transform: `translate(-50%, -50%) rotate(${pdfRotation}deg)`,
-                                                                                position: 'absolute',
-                                                                                top: '50%',
-                                                                                left: '50%',
-                                                                                transformOrigin: 'center center'
-                                                                              }
-                                                                            : {
-                                                                                width: '100%',
-                                                                                height: '100%',
-                                                                                transform: `rotate(${pdfRotation}deg)`,
-                                                                                transformOrigin: 'center center'
-                                                                              }
+                                                                        pdfRotation === 0 ? { width: '100%', height: '100%' }
+                                                                        : pdfRotation === 180 ? { width: '100%', height: '100%', transform: 'rotate(180deg)', transformOrigin: 'center center', transformStyle: 'preserve-3d' }
+                                                                        // 90°/270°: translate and absolute positioning fixes the clipping.
+                                                                        // Using 100vh and 100vw guarantees it fills the screen rather than being constrained by the parent's width/height which are swapped.
+                                                                        // Note: Because it's within a flex container, relying purely on vh/vw can sometimes overflow or underflow slightly if headers are present.
+                                                                        // However, using the parent dimensions via a ResizeObserver or inline math is often more robust.
+                                                                        // Let's use standard width/height swap but rely on the parent container dimensions to be 100% of the flex-1 area.
+                                                                        : { width: '100vh', height: '100vw', transform: `translate(-50%, -50%) rotate(${pdfRotation}deg)`, position: 'absolute', top: '50%', left: '50%', transformOrigin: 'center center', transformStyle: 'preserve-3d' }
                                                                     }
                                                                 />
